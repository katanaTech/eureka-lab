# Sprint 1 Plan — Foundation

**Goal:** Working monorepo with both apps building and running locally. No features yet — just infrastructure.

**Exit Criteria:**
- [ ] `pnpm install` + `pnpm build` works from root
- [ ] `pnpm dev` starts both apps concurrently
- [ ] Backend `/health` returns `{"status":"ok"}`
- [ ] Frontend renders a page at `localhost:3000`
- [ ] Shared types are importable from both apps
- [ ] ESLint + Prettier pass on all files

---

*Last updated: 2026-02-19*
