'use client';

/**
 * Mobile shop card sub-components for the per-realm campaign shop.
 * Re-exports from the global mobile shop cards (identical UI).
 */

export { MobileAbilityCard as RealmAbilityCard, MobileWeaponCard as RealmWeaponCard } from '@/app/(mobile)/m/g/shop/mobile-shop-cards';
