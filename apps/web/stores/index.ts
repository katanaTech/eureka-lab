/**
 * Centralized re-export for all Zustand stores.
 * Import stores from this file rather than individual store files
 * to keep imports consistent across the codebase.
 *
 * @module stores
 */

export { useAuthStore } from './auth-store';
